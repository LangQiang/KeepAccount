<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <import type="com.godq.im.IMManager"/>
        <import type="android.view.View"/>
        <import type="com.godq.im.chatroom.MessageEntity"/>
        <variable
            name="msg"
            type="MessageEntity" />
    </data>

    <FrameLayout
        android:padding="15dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <LinearLayout
            android:visibility="@{(IMManager.INSTANCE.isMineSend(msg)) ? View.GONE : View.VISIBLE}"
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <TextView
                android:text="@{msg.userId}"
                android:textSize="12dp"
                android:textColor="@color/black60"
                android:layout_width="100dp"
                android:layout_height="wrap_content" />

            <TextView
                android:text="@{msg.msg}"
                android:background="@color/white"
                android:textColor="@color/black"
                android:layout_marginTop="6dp"
                android:textSize="16dp"
                android:paddingTop="8dp"
                android:paddingBottom="8dp"
                android:paddingStart="10dp"
                android:paddingEnd="10dp"
                android:layout_marginEnd="100dp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />
        </LinearLayout>

        <LinearLayout
            android:visibility="@{!IMManager.INSTANCE.isMineSend(msg) ? View.GONE : View.VISIBLE}"
            android:layout_gravity="end"
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <TextView
                android:text="@{msg.userId}"
                android:textSize="12dp"
                android:textColor="@color/black60"
                android:layout_gravity="end"
                android:maxWidth="100dp"
                android:ellipsize="end"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />

            <TextView
                android:text="@{msg.msg}"
                android:background="#a9ea7a"
                android:textColor="@color/black"
                android:layout_gravity="end"
                android:layout_marginTop="6dp"
                android:textSize="16dp"
                android:paddingTop="8dp"
                android:paddingBottom="8dp"
                android:paddingStart="10dp"
                android:paddingEnd="10dp"
                android:layout_marginStart="100dp"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />
        </LinearLayout>
    </FrameLayout>


</layout>